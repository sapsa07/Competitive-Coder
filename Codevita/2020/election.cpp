#include<iostream>
#include<string>
using namespace std;

#define ll long long

int main()
{
    // ...
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // ...

    ll size; // length of queue of voters.
    cin >> size;

    string voters;

    int j = 0;

    while(voters[j] != '\0') {
        
    }

    return 0;
}